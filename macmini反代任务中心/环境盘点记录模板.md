# 环境盘点记录模板

## 1. 使用说明

这份模板用于真实盘点 `mac mini` 当前环境。  
目标不是分析方案，而是把现状记录清楚，作为后续部署与隔离设计的事实基础。

使用原则：

1. 只记录真实情况
2. 不靠猜
3. 不先假设代理或 Docker 一定没问题
4. 每一项能填就填，不能填就标记为“待确认”

## 2. 机器基础信息

### 2.1 主机信息

- 主机名称：
- 本机用户名：
- macOS 版本：
- 芯片类型：
- 内存容量：
- 磁盘总容量：
- 当前可用磁盘：

### 2.2 网络基础

- 当前主要联网方式：
- 当前本机局域网 IP：
- 当前公网入口是否已存在：
- 是否已安装 Cloudflare Tunnel：
- 是否已安装 Docker Desktop：

## 3. Docker 环境盘点

### 3.1 当前运行中的容器

按容器逐项填写：

#### 容器 1

- 容器名：
- 镜像：
- 状态：
- 是否关键任务：
- 是否绝对不能重启：
- 占用端口：
- 备注：

#### 容器 2

- 容器名：
- 镜像：
- 状态：
- 是否关键任务：
- 是否绝对不能重启：
- 占用端口：
- 备注：

#### 容器 3

- 容器名：
- 镜像：
- 状态：
- 是否关键任务：
- 是否绝对不能重启：
- 占用端口：
- 备注：

如有更多容器，继续往下补。

### 3.2 Compose 项目

#### 项目 1

- Compose 项目名：
- 关联容器：
- 是否关键项目：
- 备注：

#### 项目 2

- Compose 项目名：
- 关联容器：
- 是否关键项目：
- 备注：

### 3.3 Docker 网络

#### 网络 1

- 网络名：
- 驱动：
- 是否被关键容器使用：
- 备注：

#### 网络 2

- 网络名：
- 驱动：
- 是否被关键容器使用：
- 备注：

### 3.4 Docker 卷

#### 卷 1

- 卷名：
- 是否关键数据卷：
- 关联容器：
- 备注：

#### 卷 2

- 卷名：
- 是否关键数据卷：
- 关联容器：
- 备注：

## 4. 端口占用盘点

重点记录当前机器上重要端口占用情况。

### 4.1 任务中心拟使用端口

- `18080` 是否空闲：
- `16379` 是否空闲：
- `19090` 是否空闲：

### 4.2 当前已占用的重要端口

#### 端口 1

- 端口号：
- 对应进程或容器：
- 用途：
- 是否可替换：

#### 端口 2

- 端口号：
- 对应进程或容器：
- 用途：
- 是否可替换：

#### 端口 3

- 端口号：
- 对应进程或容器：
- 用途：
- 是否可替换：

## 5. 代理与 VPN 盘点

这是最关键的一部分，必须真实记录。

### 5.1 Codex 当前联网方式

- Codex 当前是否依赖 VPN：
- Codex 当前是否依赖系统代理：
- Codex 当前是否依赖 shell 环境变量代理：
- Codex 当前是否依赖第三方代理客户端：
- 当前是否能明确说出是哪一种：

### 5.2 代理变量现状

- `HTTP_PROXY` 当前是否存在：
- `HTTPS_PROXY` 当前是否存在：
- `ALL_PROXY` 当前是否存在：
- `NO_PROXY` 当前是否存在：

如果存在，请记录来源：

- 写在 shell profile：
- 写在系统环境：
- 写在某个启动脚本：
- 写在 Docker Desktop：
- 待确认：

### 5.3 VPN 或代理客户端信息

- 使用的 VPN/代理工具名称：
- 当前是否常驻运行：
- 是否整机全局代理：
- 是否仅浏览器代理：
- 是否仅终端代理：
- 是否仅某个客户端代理：

### 5.4 代理影响范围判断

- Docker 容器是否会继承这些代理：
- 本地命令行是否默认带代理：
- SSH 登录环境是否带代理：
- `cloudflared` 未来是否可能误继承代理：
- 待确认项：

## 6. Codex 运行环境盘点

### 6.1 Codex 基础情况

- Codex 安装方式：
- 启动方式：
- 是否长期驻留：
- 是否在 tmux 中运行：
- 是否依赖固定 shell 环境：

### 6.2 Codex 代理耦合情况

- 不开 VPN/代理时 Codex 是否还能正常工作：
- 当前 Codex 是否必须带代理：
- 是否已有专门的代理启动方式：
- 是否已有 `NO_PROXY` 配置：

## 7. 资源余量盘点

### 7.1 CPU

- 当前空闲情况：
- 高峰时是否常年吃满：
- 现有 Docker 任务是否重 CPU：
- 是否还能承受额外 4 个槽位：

### 7.2 内存

- 当前内存占用：
- 高峰时内存占用：
- Docker 已占用大概多少：
- 是否还有足够余量给 Redis + API + worker：

### 7.3 磁盘

- 当前可用空间：
- 是否有大日志或素材目录：
- 是否适合新增任务结果目录：
- 是否需要单独规划结果存储路径：

## 8. 风险点记录

把盘点过程中发现的风险单独记录。

### 风险 1

- 风险描述：
- 影响范围：
- 是否阻塞项目：
- 建议处理方式：

### 风险 2

- 风险描述：
- 影响范围：
- 是否阻塞项目：
- 建议处理方式：

### 风险 3

- 风险描述：
- 影响范围：
- 是否阻塞项目：
- 建议处理方式：

## 9. 阶段结论

盘点完成后，必须给出一个明确结论。

### 9.1 当前是否适合进入下一阶段

- 是否可以进入“架构验收”：
- 是否可以进入“单槽验证准备”：
- 是否存在必须先解决的问题：

### 9.2 当前最关键的未决问题

1.
2.
3.

### 9.3 建议下一步

1.
2.
3.

## 10. 快速汇总版

如果要快速给别人同步，只提炼这几项：

- 最不能碰的 Docker 容器：
- 当前代理方式：
- Codex 是否强依赖代理：
- 任务中心拟用端口是否空闲：
- 机器资源是否够用：
- 当前最大的 3 个风险：
